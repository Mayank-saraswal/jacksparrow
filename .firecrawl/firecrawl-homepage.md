# Scrape evidence — firecrawl.dev (homepage)

- Captured: 2026-06-12
- Tool: web fetch (rendered). NOTE: the `firecrawl` CLI and `FIRECRAWL_API_KEY`
  were not available in this environment, so the structured `branding` token
  block and the full-page screenshot specified by the skill could not be
  captured. Colors/spacing below are inferred from the rendered page and
  Firecrawl's public brand, and are labelled as such in DESIGN.md.

## Observed structure / copy

- Hero: "Power AI agents with clean web data" / "The API to search, scrape, and
  interact with the web at scale." + "It's also open source."
- Primary CTA: "Start scraping" / "Start for free". Secondary: "See our plans".
- Section meta-labels in monospace with numbering and slashes, e.g.
  `[ 01 / 06 ] · MAIN FEATURES`, `// Developer First //`, `// Agent Ready //`,
  `// Built for Performance //`, `// Zero configuration //`, `// Use cases //`,
  `// Community //`, `// Get started //`, `// FAQ //`.
- Bold, short section headings: "Start scraping today", "We handle the hard
  stuff", "Fast, reliable, and easy to integrate."
- Dark code/terminal panels showing JSON / Markdown / Python / cURL / CLI tabs.
- "Trusted by 150,000+ companies" logo cloud (greyscale logos).
- Comparison bars (Firecrawl 89% vs Puppeteer 74% vs cURL 71%).
- Testimonials as quote cards; FAQ as accordion grouped by category.
- Footer: "The easiest way to extract data from the web", backed-by, social,
  SOC2 badge, "All systems normal" status pill, © 2026 Firecrawl.

## Visual language (observed + inferred)

- Clean, near-white background; high-contrast near-black text.
- Warm orange/flame brand accent (the Firecrawl flame). CTAs and highlights orange.
- Heavy use of monospace for meta-labels, code, and numeric callouts — a
  developer/technical aesthetic.
- Rounded cards with subtle 1px borders and soft shadows; generous whitespace.
- Dark panels for code, light panels for content.
